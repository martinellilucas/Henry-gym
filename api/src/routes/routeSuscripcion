const handlerGetSuscripcion = require("../handlers/handlerGetSuscripcion");
const { Router } = require("express");
const routeSuscripcion = Router();

routeSuscripcion.get("/", handlerGetSuscripcion);

module.exports = routeSuscripcion;
